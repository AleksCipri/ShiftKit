torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
matplotlib>=3.7.0
scikit-learn>=1.2.0
tqdm>=4.65.0

# Optional — required only for shiftkit.models.GNN (PyTorch Geometric)
# torch-geometric>=2.4.0

# Optional — required only for UMAP projection in diagnostics
# umap-learn>=0.5.0

# Optional — required only for SIDDATrainer
# geomloss>=0.2.6
